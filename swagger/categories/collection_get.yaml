tags:
- Categories
description: Get a list of categories
operationId: collection_get_categories
parameters:
- in: query
  name: offset
  schema:
    type: string
  description: Offset
- in: query
  name: limit
  schema:
    type: int
  description: Limit
- in: query
  name: reverse
  schema:
    type: int
  description: Descending order
produces:
- application/json
responses:
  "200":
    description: successful operation
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/PaginatedList'
