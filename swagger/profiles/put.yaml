tags:
- Profiles
description: Profile create
operationId: collection_put_profile
security:
  - Basic: []
requestBody:
  description: Profile data
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/Input_ProfileCreateData_'
responses:
  "200":
    description: successful operation
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Response_Profile_'
  "400":
    description: validation errors
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'