tags:
- Products
description: Product create
operationId: collection_put_product
security:
  - Basic: []
requestBody:
  description: Product data
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/Input_ProductCreateData_'
responses:
  "200":
    description: successful operation
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Response_Product_'
  "400":
    description: validation errors
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'