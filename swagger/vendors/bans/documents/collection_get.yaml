tags:
- Vendors/Bans
description: Get a list of vendor ban documents
operationId: collection_get_vendor_ban_docs
parameters:
- in: path
  name: vendor_id
  schema:
    type: string
  required: true
  description: ID of the vendor
- in: path
  name: ban_id
  schema:
    type: string
  required: true
  description: ID of the vendor ban
produces:
- application/json
responses:
  "200":
    description: successful operation
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ListResponse_Document_'
  "500":
    description: Server error
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'