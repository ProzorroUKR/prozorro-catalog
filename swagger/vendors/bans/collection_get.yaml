tags:
- Vendors/Bans
description: Get a list of vendor bans
operationId: collection_get_vendor_bans
parameters:
- in: path
  name: vendor_id
  schema:
    type: string
  required: true
  description: ID of the vendor
produces:
- application/json
responses:
  "200":
    description: successful operation
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ListResponse_Ban_'
  "500":
    description: Server error
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'